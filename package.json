{
  "name": "jukebox",
  "version": "0.1.0",
  "license": "ISC",
  "description": "online music player / backup",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "cd packages/jukebox-utils && yarn test",
    "files": "http-server temp --cors=If-Modified-Since, Range",
    "player": "cd packages/player && yarn start"
  },
  "dependencies": {
    "http-server": "^0.11.1",
    "lerna": "^3.10.7"
  }
}
